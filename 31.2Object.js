var cuboid = {
  length: 25,
  width: 50,
  height: 200,
};

//your code goes here
function object(length, width, height) {
  var volume = length * width * height;
  return volume;
}

console.log(object(25, 50, 200));
